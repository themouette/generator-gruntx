/* global requireBower */
// this is the bootstrap javascript inclusion.
function requireBootstrap(part) {
    'use strict';
    return requireBower('bootstrap/js/bootstrap-' + part);
}
// requireBootstrap('bootstrap/affix');
// requireBootstrap('bootstrap/transition');
// requireBootstrap('bootstrap/alert');
// requireBootstrap('bootstrap/button');
// requireBootstrap('bootstrap/collapse');
// requireBootstrap('bootstrap/dropdown');
// requireBootstrap('bootstrap/modal');
// requireBootstrap('bootstrap/tooltip');
// requireBootstrap('bootstrap/popover');
// requireBootstrap('bootstrap/scrollspy');
// requireBootstrap('bootstrap/tab');
// requireBootstrap('bootstrap/typeahead');
